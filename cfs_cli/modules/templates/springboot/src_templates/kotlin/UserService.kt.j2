// ============= UserService.kt =============
package {{ package_name }}.services

import {{ package_name }}.dtos.UserDTO
import {{ package_name }}.dtos.UserResponseDTO

/**
 * Example service interface. This interface should be implemented by the service-implementation layer.
 * It only serves as a template for defining business-logic methods.
 */
interface UserService {
    fun findAllUsers(): List<UserResponseDTO>
    fun findById(id: Long): UserResponseDTO
    fun createUser(userDTO: UserDTO): UserResponseDTO
    fun updateUser(id: Long, userDTO: UserDTO): UserResponseDTO
    fun deleteUser(id: Long)
}